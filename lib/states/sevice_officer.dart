import 'package:flutter/material.dart';

class ServiceOffice extends StatelessWidget {
  const ServiceOffice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}